{
    "modules": [
        {
            "data": {
                "busy_connection": "${USER}_test.df_busy_signal",
                "dataflow_applications": [
                    {
                        "decision_connection": "${USER}_test.trigdec_0",
                        "thresholds": {
                            "busy": 10,
                            "free": 5
                        }
                    }
                ],
                "general_queue_timeout": 100,
                "td_connection": "${USER}_test.td_mlt_to_dfo",
                "td_send_retries": 5,
                "token_connection": "${USER}_test.triginh"
            },
            "match": "dfo"
        }
    ]
}