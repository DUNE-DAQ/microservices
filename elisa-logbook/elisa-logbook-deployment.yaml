---
apiVersion: v1
kind: Namespace
metadata:
  annotations:
    kluctl.io/skip-delete-if-tags: "true"
  labels:
    pod-security.kubernetes.io/audit: baseline
    pod-security.kubernetes.io/audit-version: latest
    pod-security.kubernetes.io/enforce: baseline # unified image runs as root :(
    pod-security.kubernetes.io/enforce-version: latest
    pod-security.kubernetes.io/warn: baseline
    pod-security.kubernetes.io/warn-version: latest
  name: elisa-logbook
---
# You must still create a secret called elisa-logbook-secret with the required keys
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/app: elisa-logbook
    app.kubernetes.io/component: elisa-logbook
  name: elisa-logbook
  namespace: elisa-logbook
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/app: elisa-logbook
      app.kubernetes.io/component: elisa-logbook
  template:
    metadata:
      labels:
        app.kubernetes.io/app: elisa-logbook
        app.kubernetes.io/component: elisa-logbook
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: node-role.kubernetes.io/worker
                operator: Exists
      automountServiceAccountToken: false
      containers:
      - image: ghcr.io/dune-daq/microservices:latest
        name: elisa-logbook
        env:
        - name: MICROSERVICE
          value: elisa-logbook
        - name: HARDWARE
          valueFrom:
            secretKeyRef:
              key: hardware
              name: elisa-logbook-secret 
        - name: USERNAME
          valueFrom:
            secretKeyRef:
              key: username
              name: elisa-logbook-secret 
        - name: PASSWORD
          valueFrom:
            secretKeyRef:
              key: password
              name: elisa-logbook-secret
        resources:
          limits:
            memory: 1Gi
          requests:
            memory: 8Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          runAsGroup: 11000
          seccompProfile:
            type: RuntimeDefault
      securityContext:
        fsGroup: 11000
